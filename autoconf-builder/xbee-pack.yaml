schema-version: 1.0

type: builder


const:
  srcDir: /usr/src

out:
  autoconf: /opt/autoconf

dependency: m4

provision:
  - url:
      from: https://ftp.gnu.org/gnu/autoconf/autoconf-{% version %}.tar.gz
      todir: "{% srcDir %}"

build:
  - shell:
      cmds:
        - ./configure --prefix=/usr
        - make
#        - make check
        - make install DESTDIR={% autoconf %}
      directory: "{% srcDir %}/autoconf-{% version %}"


